import {makeStyles} from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  mainContainer: {
    display: 'flex',
    alignItems: 'center',
    paddingTop: '20px',
  },
  smMargin: {
    margin: theme.spacing(1),
  },
  about: {
    marginTop: '15px',
    marginBottom: '15px',

  },

}));
